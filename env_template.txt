# Anthropic API Configuration (Primary)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Specify model (defaults to claude-3-5-sonnet-20241022)
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Optional: Set temperature for response generation (0.0 to 1.0)
ANTHROPIC_TEMPERATURE=0.1

# Optional: Set max context length for response (defaults to 200000)
ANTHROPIC_MAX_CONTEXT_LENGTH=200000

# OpenAI API Configuration (Alternative)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify model (defaults to gpt-4o)
OPENAI_MODEL=gpt-4o

# Optional: Set temperature for response generation (0.0 to 1.0)
OPENAI_TEMPERATURE=0.1

# Optional: Set max context length for response (defaults to 128000)
OPENAI_MAX_CONTEXT_LENGTH=128000
